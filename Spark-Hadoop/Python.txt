  spark-python-wf:
    build:
      context: template/
      args:  
        SPARK_VERSION: ${SPARK_VERSION}
        HADOOP_VERSION: ${HADOOP_VERSION}
    image: spark-python-wf:${SPARK_VERSION}-hadoop${HADOOP_VERSION}
    container_name: spark-python-wf
    depends_on:
      - spark-master
    environment:
      ENABLE_INIT_DAEMON: "false"
      SPARK_MASTER_NAME: ${SPARK_MASTER_NAME}
      SPARK_MASTER_PORT: ${SPARK_MASTER_PORT}
      SPARK_SUBMIT_ARGS: ${SPARK_SUBMIT_ARGS}
      #EXECUTOR_MEMORY: ${EXECUTOR_MEMORY}
      SPARK_APPLICATION_PYTHON_LOCATION: ${SPARK_APPLICATION_PYTHON_LOCATION_WF}
      SPARK_APPLICATION_ARGS: ${SPARK_APPLICATION_ARGS}
    links:
      - spark-master:spark-master
    networks:
      PLICA:
        ipv4_address: 172.22.0.4